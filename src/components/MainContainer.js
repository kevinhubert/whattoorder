import React from "react";

class MainContainer extends React.Component {
  render() {
    return (
      <main>
        <h2>Main Container Title</h2>
        <p>This is where the menu items will go for each restaurant.</p>
      </main>
    );
  }
}

export default MainContainer;
